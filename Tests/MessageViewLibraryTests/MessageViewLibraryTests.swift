import XCTest
@testable import MessageViewLibrary

final class MessageViewLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MessageViewLibrary().text, "Hello, World!")
    }
}
